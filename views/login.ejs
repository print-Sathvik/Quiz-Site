<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content= "<%= csrfToken %>">
    <link href="/css/styles.css" rel="stylesheet" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    
</head>
<body class="bodyst">
    <%- include("header.ejs") %>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
          <h6 class="py-4">Login to your account</h6>
          <form action= <%= formAction %> method="post">
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="flash-error"> <%= messages.error[i] %></p>
                <% } %>
              <% } %>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="py-3">
                <% if(formAction == "/session")  { %>
                    <label for="email">Email:</label>
                    <input type="email" name="email" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                <% } else { %>
                    <label for="voterid">Voter ID:</label>
                    <input type="text" name="voterid" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                <% } %>
            </div>  
            <div class="py-3"> 
                <label for="password">Password:</label>
                <input type="password" name="password" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
            </div>  
            <div class="py-3 centerElement"> 
                <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Sign in</button>        
            </div>  
            </form>
        </div>
    </div>
</body>
</html>