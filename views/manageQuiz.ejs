<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <link href="/css/styles.css" rel="stylesheet">
    <title>Decipher</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <%- include('header.ejs') %>
    <div class="grid grid-cols-6">
        <div class="col-start-2 col-span-3">
            <h1 class="text-2xl py-4 centerElement"><%= quizTitle %></h1>
            <% if(messages && messages.error && messages.error.length > 0)  { %>
                <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="flash-error"> <%= messages.error[i] %></p>
                <% } %>
            <% } %>
            <div class="flex gap-2 py-2 justify-center">
                <div class="flex-none">
                    <a href="/quiz/manage/<%= quizId %>/edit"><button class="bg-green-600 text-white px-5 py-2 rounded font-medium mr-2 mb-2"> Edit Details </button></a>
                </div>
            </div>
            <h3 class="text-xl py-2">Share this Key with the participants: <span class="text-red-600 text-xl"><%= key %></span></h3>
            <p class="text-lg">Quiz Duration in seconds: <%= timer %> seconds</p>
            <p class="text-lg">Score of each question: <%= score %></p>
            <p class="text-lg">Penalty for each hint: <%=penalty%></p>
            <div>
                <p class="text-green-600 text-lg py-6"><a href="/quiz/manage/<%= quizId %>/managePlayers">Manage Participants</a></p>
                <p class="text-green-600 text-lg py-6"><a href="/quiz/manage/<%= quizId %>/manageQuestions">Manage Questions</a></p>
            </div>
        </div>
    </div>
</body>
</html>